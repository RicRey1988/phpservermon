# commit-message: refactor: migrate user list to native Hope UI
diff --git a/src/templates/default/module/user/user/list.tpl.html b/src/templates/default/module/user/user/list.tpl.html
index 5e99777..452c2c6 100644
--- a/src/templates/default/module/user/user/list.tpl.html
+++ b/src/templates/default/module/user/user/list.tpl.html
@@ -10,25 +10,25 @@
   </form>
   {% if pending_invitations %}<div class="card-footer"><p class="small text-muted mb-2">Invitaciones pendientes</p><div class="d-flex flex-wrap gap-2">{% for invitation in pending_invitations %}<form action="{{ invitation.url_revoke|raw }}" method="post" class="d-inline-flex align-items-center gap-1 badge bg-soft-info text-info">{{ macro.input_csrf() }}<span>{{ invitation.email }} · {{ invitation.expires_at }}</span><input type="hidden" name="invitation_id" value="{{ invitation.invitation_id }}"><button class="btn btn-sm btn-link text-danger p-0" type="submit" aria-label="Revocar invitación">×</button></form>{% endfor %}</div></div>{% endif %}
 </section>
-<div class="searchbar input-group mb-3">
-  <span class="input-group-text">{{ hope.icon('search') }}</span>
-  <input class="form-control" type="search" maxlength="255" placeholder="{{ label_search }}..." aria-label="{{ label_search }}" data-card-search>
+<div class="input-group mb-3">
+  <span class="input-group-text" id="user-search-label">{{ hope.icon('search') }}</span>
+  <input class="form-control" type="search" maxlength="255" placeholder="{{ label_search }}..." aria-labelledby="user-search-label" data-card-search>
 </div>
 <div class="row row-cols-1 row-cols-lg-2 row-cols-xxl-3 g-3" data-card-collection>
   {% for user in users %}
   <div class="col" data-search-text="{{ user.user_name }} {{ user.name }} {{ user.email }} {{ user.mobile }}">
-    <article class="card user-card h-100">
+    <article class="card h-100" data-user-card>
       <div class="card-body d-flex flex-column">
         <div class="d-flex align-items-start justify-content-between gap-3 mb-3">
           <div class="d-flex align-items-center gap-3">
-            <span class="avatar bg-soft-primary text-primary" aria-hidden="true">{{ hope.icon(user.level == '10' ? 'users' : 'user') }}</span>
+            <span class="avatar-50 rounded-circle bg-soft-primary text-primary d-inline-flex align-items-center justify-content-center" aria-hidden="true">{{ hope.icon(user.level == '10' ? 'users' : 'user') }}</span>
             <div><h2 class="h5 mb-1">{{ user.name }}</h2><p class="text-muted mb-0">@{{ user.user_name }}</p></div>
           </div>
           <span class="badge bg-soft-primary text-primary">{{ user.level_text }}</span>
         </div>
-        <dl class="user-contact mb-3">
-          <div><dt>{{ label_email }}</dt><dd>{{ user.email ?: '—' }}</dd></div>
-          <div><dt>{{ label_mobile }}</dt><dd>{{ user.mobile ?: '—' }}</dd></div>
+        <dl class="list-group list-group-flush mb-3">
+          <div class="list-group-item px-0 d-flex justify-content-between gap-3"><dt>{{ label_email }}</dt><dd class="mb-0 text-break text-end">{{ user.email ?: '—' }}</dd></div>
+          <div class="list-group-item px-0 d-flex justify-content-between gap-3"><dt>{{ label_mobile }}</dt><dd class="mb-0 text-break text-end">{{ user.mobile ?: '—' }}</dd></div>
         </dl>
         <div class="mb-3">
           <p class="small text-uppercase text-muted fw-semibold mb-2">{{ label_servers }}</p>
