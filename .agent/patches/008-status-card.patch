# commit-message: refactor: migrate status cards
diff --git a/src/templates/default/module/server/status/cards.tpl.html b/src/templates/default/module/server/status/cards.tpl.html
index fdf616e..757f8e8 100644
--- a/src/templates/default/module/server/status/cards.tpl.html
+++ b/src/templates/default/module/server/status/cards.tpl.html
@@ -1,26 +1,38 @@
 {% macro server_card(server, compact, label_last_online, label_last_offline, label_last_check, label_rtime) %}
-<article class="card status-card status-{{ server.status_tone }}{% if compact %} status-card--compact{% endif %}" data-server-id="{{ server.server_id }}">
-    <div class="card-body d-flex flex-column min-w-0">
-        <div class="status-banner status-banner--{{ server.status_tone }}" role="status" aria-label="Estado: {{ server.status_label }}"><span class="status-banner-icon" data-server-status-icon aria-hidden="true">{{ server.status_tone == 'online' ? '✓' : (server.status_tone == 'offline' ? '×' : (server.status_tone == 'warning' ? '!' : 'Ⅱ')) }}</span><strong data-server-status-label>{{ server.status_label|upper }}</strong><span class="status-pulse" aria-hidden="true"></span></div>
-        <div class="server-image-box mb-3">
-            <img src="{{ server.image_url }}" width="96" height="96" alt="" loading="lazy" decoding="async">
-        </div>
-        <div class="d-flex justify-content-between align-items-start gap-2 mb-3">
-            <div class="min-w-0">
-                <h2 class="h5 status-card-title mb-1"><a class="stretched-link text-decoration-none" href="{{ server.url_view|raw }}">{{ server.label }}</a></h2>
-                <small class="text-muted text-break d-block">{{ server.ip }}{% if server.port %}:{{ server.port }}{% endif %}</small>
+{% set tones = {'online':'success','offline':'danger','warning':'warning','paused':'secondary'} %}
+{% set tone = tones[server.status_tone]|default('secondary') %}
+{% set symbol = server.status_tone == 'online' ? '✓' : (server.status_tone == 'offline' ? '×' : (server.status_tone == 'warning' ? '!' : 'Ⅱ')) %}
+<article class="card h-100 overflow-hidden border-top border-4 border-{{ tone }}" data-server-card data-server-id="{{ server.server_id }}">
+    <div class="card-header bg-{{ tone }} text-white d-flex align-items-center gap-2" role="status" aria-label="Estado: {{ server.status_label }}">
+        <span class="d-inline-flex align-items-center justify-content-center rounded-circle border border-light" data-server-status-icon aria-hidden="true">{{ symbol }}</span>
+        <strong data-server-status-label>{{ server.status_label|upper }}</strong>
+    </div>
+    <div class="card-body">
+        <div class="row g-3 align-items-center">
+            <div class="{{ compact ? 'col-12 col-sm-3 text-center' : 'col-12 text-center' }}">
+                <span class="d-inline-flex align-items-center justify-content-center rounded border bg-body p-3" data-server-image>
+                    <img class="img-fluid" src="{{ server.image_url }}" width="96" height="96" alt="" loading="lazy" decoding="async">
+                </span>
+            </div>
+            <div class="{{ compact ? 'col-12 col-sm-9' : 'col-12' }}">
+                <div class="d-flex justify-content-between align-items-start gap-3 mb-3">
+                    <div>
+                        <h2 class="h5 mb-1"><a class="stretched-link text-decoration-none text-break" href="{{ server.url_view|raw }}">{{ server.label }}</a></h2>
+                        <small class="text-muted text-break d-block">{{ server.ip }}{% if server.port %}:{{ server.port }}{% endif %}</small>
+                    </div>
+                    <span class="badge bg-{{ tone }}" aria-label="{{ server.status_label }}">{{ server.status_label }}</span>
+                </div>
+                <ul class="list-group list-group-flush mb-0">
+                    <li class="list-group-item px-0 d-flex justify-content-between gap-3"><span class="text-muted">{{ label_last_online }}</span><strong class="text-end" data-server-last-online>{{ server.last_online_nice }}</strong></li>
+                    {% if server.status_tone == 'online' %}
+                    <li class="list-group-item px-0 d-flex justify-content-between gap-3"><span class="text-muted">{{ label_last_offline }}</span><strong class="text-end">{{ server.last_offline_nice }} {{ server.last_offline_duration_nice }}</strong></li>
+                    <li class="list-group-item px-0 d-flex justify-content-between gap-3"><span class="text-muted">{{ label_rtime }}</span><strong class="text-end" data-server-latency>{{ (server.rtime * 1000)|round(2) }} ms</strong></li>
+                    {% else %}
+                    <li class="list-group-item px-0 d-flex justify-content-between gap-3"><span class="text-muted">{{ label_last_check }}</span><strong class="text-end" data-server-last-check>{{ server.last_checked_nice }}</strong></li>
+                    {% endif %}
+                </ul>
             </div>
-            <span class="badge status-badge status-badge--{{ server.status_tone }}" aria-label="{{ server.status_label }}">{{ server.status_label }}</span>
         </div>
-        <dl class="server-facts mb-0">
-            <div><dt>{{ label_last_online }}</dt><dd data-server-last-online>{{ server.last_online_nice }}</dd></div>
-            {% if server.status_tone == 'online' %}
-            <div><dt>{{ label_last_offline }}</dt><dd>{{ server.last_offline_nice }} {{ server.last_offline_duration_nice }}</dd></div>
-            <div><dt>{{ label_rtime }}</dt><dd data-server-latency>{{ (server.rtime * 1000)|round(2) }} ms</dd></div>
-            {% else %}
-            <div><dt>{{ label_last_check }}</dt><dd data-server-last-check>{{ server.last_checked_nice }}</dd></div>
-            {% endif %}
-        </dl>
     </div>
 </article>
 {% endmacro %}
