# commit-message: refactor: migrate PHP diagnostics cards
--- a/src/templates/default/module/config/config.tpl.html
+++ b/src/templates/default/module/config/config.tpl.html
@@ -304,11 +304,11 @@
                 <legend class="card-header h5 float-none w-100 mb-0">{{ label_tab_php_info }}</legend>
                 <div class="card-body">
                 <p>{{ label_php_info_safe }}</p>
-                <div class="php-info-grid row row-cols-1 row-cols-xl-2 g-4">
+                <div class="row row-cols-1 row-cols-xl-2 g-4" data-php-info>
                     {% for group, values in php_info %}
-                    <div class="col"><section class="card h-100 php-info-card"><div class="card-header"><h3 class="h5 mb-0">{{ {'runtime':'Entorno de ejecución','limits':'Límites y cargas','acceleration':'Rendimiento','platform':'Compatibilidad'}[group]|default(group|title) }}</h3></div><div class="card-body"><dl class="server-facts mb-0">
-                        {% for key, value in values %}<div><dt>{{ key|replace({'_':' '})|title }}</dt><dd class="text-break">{% if value is iterable %}{% if value.enabled is defined %}<span class="badge bg-{{ value.enabled ? 'success' : 'warning' }} me-1">{{ value.enabled ? 'ACTIVO' : 'INACTIVO' }}</span>{% endif %}<code>{{ value|json_encode|e }}</code>{% else %}{{ value|e }}{% endif %}</dd></div>{% endfor %}
-                    </dl></div></section></div>
+                    <div class="col"><section class="card h-100"><div class="card-header"><h3 class="h5 mb-0">{{ {'runtime':'Entorno de ejecución','limits':'Límites y cargas','acceleration':'Rendimiento','platform':'Compatibilidad'}[group]|default(group|title) }}</h3></div><ul class="list-group list-group-flush">
+                        {% for key, value in values %}<li class="list-group-item d-flex flex-column flex-sm-row justify-content-between gap-2"><span class="text-muted">{{ key|replace({'_':' '})|title }}</span><span class="text-break text-sm-end">{% if value is iterable %}{% if value.enabled is defined %}<span class="badge bg-{{ value.enabled ? 'success' : 'warning' }} me-1">{{ value.enabled ? 'ACTIVO' : 'INACTIVO' }}</span>{% endif %}<code>{{ value|json_encode|e }}</code>{% else %}{{ value|e }}{% endif %}</span></li>{% endfor %}
+                    </ul></section></div>
                     {% endfor %}
                 </div>
                 </div>
