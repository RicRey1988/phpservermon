# commit-message: refactor: migrate system diagnostics views
diff --git a/src/templates/default/module/config/system-updated.tpl.html b/src/templates/default/module/config/system-updated.tpl.html
index 5972e79..6f77173 100644
--- a/src/templates/default/module/config/system-updated.tpl.html
+++ b/src/templates/default/module/config/system-updated.tpl.html
@@ -1,7 +1,7 @@
 {% import 'main/icons.tpl.html' as hope %}
 <section class="card col-12 col-xl-8 mx-auto text-center">
   <div class="card-body p-5">
-    <span class="avatar avatar-lg bg-soft-success text-success mb-4">{{ hope.icon('check') }}</span>
+    <span class="avatar-80 rounded-circle bg-soft-success text-success d-inline-flex align-items-center justify-content-center mb-4">{{ hope.icon('check') }}</span>
     <h1 class="h3">Archivos actualizados a {{ target_version }}</h1>
     <p class="text-muted">Se verificaron e instalaron {{ changed_files }} archivos. La configuración, los registros y las imágenes se conservaron.</p>
     {% if database_upgrade_required %}
diff --git a/src/templates/default/module/config/system.tpl.html b/src/templates/default/module/config/system.tpl.html
index c8f25cf..2b8707d 100644
--- a/src/templates/default/module/config/system.tpl.html
+++ b/src/templates/default/module/config/system.tpl.html
@@ -10,8 +10,8 @@
           <div class="row row-cols-1 row-cols-md-2 g-3">
             {% for check in checks %}
               <div class="col"><article class="card border shadow-none h-100"><div class="card-body d-flex gap-3">
-                <span class="avatar bg-soft-{{ check.status == 'ok' ? 'success' : (check.status == 'warning' ? 'warning' : 'danger') }} text-{{ check.status == 'ok' ? 'success' : (check.status == 'warning' ? 'warning' : 'danger') }}">{{ hope.icon(check.status == 'ok' ? 'category' : 'warning') }}</span>
-                <div class="min-w-0"><h4 class="h6 mb-1">{{ check.label }}</h4><p class="fw-semibold mb-1">{{ check.value }}</p><p class="small text-muted mb-0 text-break">{{ check.detail }}</p>{% if check.status == 'error' and ('chgrp' in check.detail) %}<details class="mt-3"><summary class="text-primary fw-semibold">Cómo solucionarlo</summary><p class="small mt-2 mb-0">El proceso PHP usa un usuario distinto al propietario de los archivos. Ejecute el comando indicado como administrador; concede escritura al grupo de PHP sin hacer públicos los archivos.</p></details>{% endif %}</div>
+                <span class="avatar-40 rounded-circle d-inline-flex align-items-center justify-content-center flex-shrink-0 bg-soft-{{ check.status == 'ok' ? 'success' : (check.status == 'warning' ? 'warning' : 'danger') }} text-{{ check.status == 'ok' ? 'success' : (check.status == 'warning' ? 'warning' : 'danger') }}">{{ hope.icon(check.status == 'ok' ? 'category' : 'warning') }}</span>
+                <div class="flex-grow-1"><h4 class="h6 mb-1">{{ check.label }}</h4><p class="fw-semibold mb-1">{{ check.value }}</p><p class="small text-muted mb-0 text-break">{{ check.detail }}</p>{% if check.status == 'error' and ('chgrp' in check.detail) %}<details class="mt-3"><summary class="text-primary fw-semibold">Cómo solucionarlo</summary><p class="small mt-2 mb-0">El proceso PHP usa un usuario distinto al propietario de los archivos. Ejecute el comando indicado como administrador; concede escritura al grupo de PHP sin hacer públicos los archivos.</p></details>{% endif %}</div>
               </div></article></div>
             {% endfor %}
           </div>
@@ -20,14 +20,14 @@
     </section>
   </div>
   <div class="col-12 col-xxl-4">
-    <section class="card system-update-card sticky-xxl-top">
+    <section class="card sticky-xxl-top">
       <div class="card-header"><h2 class="h5 mb-1">Actualización desde GitHub</h2><p class="text-muted mb-0">Versión instalada: <strong>{{ current_version }}</strong></p></div>
       <div class="card-body">
         {% if release_error %}
           <div class="alert alert-warning">{{ release_error }}</div>
         {% elseif release %}
           <div class="d-flex align-items-center justify-content-between mb-3"><span>Última versión HS</span><span class="badge bg-{{ update_available ? 'success' : 'soft-primary' }}{% if not update_available %} text-primary{% endif %}">{{ release.version }}</span></div>
-          <h3 class="h6">{{ release.name }}</h3><p class="small text-muted release-notes">{{ release.notes ?: 'Sin notas adicionales.' }}</p>
+          <h3 class="h6">{{ release.name }}</h3><p class="small text-muted text-break">{{ release.notes ?: 'Sin notas adicionales.' }}</p>
           <p class="small">{{ hope.icon('shield-alt','me-2 text-success') }}El manifiesto se valida con la firma RSA de Hosting Supremo y el paquete con SHA-256 antes de instalar.</p>
           {% if update_available %}
             <form action="{{ install_url|raw }}" method="post">
