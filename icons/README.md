# Server icons

Place one PNG image per server at `icons/<server-label>.png`, using the exact configured server label as the filename.

When a matching image is unavailable, the interface displays the bundled `phpservermon.png` image.
